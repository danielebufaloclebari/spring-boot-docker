version: '3'
services:
  spring-boot-app:
    image: spring-boot-app-dummy
    ports:
      - "8081:8081"
    networks:
      - apisix-quickstart-net

networks:
  apisix-quickstart-net:
    external: true
